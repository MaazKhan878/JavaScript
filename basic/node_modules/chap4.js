let sen = "Maaz is a good man";
let word = "good";
console.log(sen.includes(word));
let d = (`"${word}" ${sen.includes(word)? "is" : "is not"}`)
let upper = (`The word ${d} in the sentence`);
console.log(upper.toUpperCase());

// amount extract
let str = "maaz have amount 100000"
let amount = str.slice("maaz have amount ".length);
console.log(Number.parseInt(amount))
// we cannot change the string charater because string as immutable

